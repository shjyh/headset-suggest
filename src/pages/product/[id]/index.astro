---
import Layout from "@/pages/_layout.astro";
import { go } from "@/proxy/backend";

const id = Astro.params.id;

const { data } = await go(Astro.request, "POST", "/headphone/detail/" + id);

console.log(data);

---

<Layout title="">

</Layout>